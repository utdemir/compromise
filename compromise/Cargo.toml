[package]
name = "compromise"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[features]
panicking = ["compromise-derive/panicking"]

[dependencies]
compromise-derive = { path = "../compromise-derive" }
